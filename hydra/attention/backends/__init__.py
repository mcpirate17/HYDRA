"""Attention backend component bundles.

This package is for backend-specific docs/benchmarks/tests that live alongside
a backend implementation.

Runtime selection is owned by `hydra.attention.registry` and construction by
`hydra.attention.factory`.
"""
