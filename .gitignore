# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# PyTorch
*.pth
*.pt
*.ckpt

# Checkpoints and model files
checkpoints/
models/
*.safetensors

# Training outputs
logs/
runs/
wandb/
tensorboard/
outputs/
reports/

# Data directories
data/
datasets/
filtered_data/
raw_data/

# Virtual environments
.venv/
venv/
env/
ENV/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Jupyter
.ipynb_checkpoints/
*.ipynb

# Environment variables
.env
.env.local

# Reports (optional - uncomment if you want to track them)
# reports/*.json
# reports/*.txt

# Temporary files
tmp/
temp/
*.tmp
*.log

# Local working-copy backups (do not commit)
hydra/kernels/lightning_attn3/ops/triton/*_working_*.py

# LA3 benchmark snapshots (keep canonical benchmark_results.json only)
hydra/kernels/lightning_attn3/docs/benchmark_results_*.json# ============================================================
# ADDITIONS TO .gitignore - Append these to existing .gitignore
# ============================================================

# Root level artifacts (often created accidentally)
nohup.out
*.deb
scaling_analysis_results.json
venv_install.log
recover_venv_complete.sh

# Diagnostics output (keep scripts in diagnostics/, ignore output)
diagnostics/output/
diagnostics/profile_*.json
diagnostics/*_trace.json

# Nested benchmark results (keep canonical benchmark_results.json only)
# Already partially covered, but be explicit:
hydra/attention/backends/*/docs/benchmark_results_*.json
hydra/attention/backends/*/docs/profile_results.json

# Triton autotune cache (machine-specific)
**/.autotune_cache/

# Full benchmark logs
hydra/attention/backends/*/full_benchmark.log
dashboard/frontend/node_modules/
